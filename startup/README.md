This script file runs on startup and is ran by the local.rc file. We've backed it up here in case anything happens to it or for new
environments. Output is sent to timestamped logs in the logs folder of this repository. The logs folder is in the .gitignore, so logs
will only show up after running the startup on the local machine.
